name: user_app
description: "Octano / Fuel & Go+ — app del cliente (Puerto Rico)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  http: ^1.2.2
  web_socket_channel: ^2.4.5
  shared_preferences: ^2.3.2
  mobile_scanner: ^5.2.3
  speech_to_text: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
